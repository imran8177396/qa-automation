/**
 * Single entry point for SEO finding-URL dedupe.
 * Implementation lives in url-normalize.ts (simple + full crawl normalizer).
 */
export { normalizeFindingUrl, normalizeCrawlUrl, normalizeUrl } from './url-normalize';
